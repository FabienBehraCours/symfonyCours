{% extends 'mi03VitrineBundle::layout.html.twig' %}

{% block content %}
    <h1>Commande edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input class="waves-effect waves-light btn-large" type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('commande_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
